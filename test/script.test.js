'use strict';

describe(
    'script', () =>{
        describe(
            '#find', () =>{
                it(
                    'should return result when a file matches a term'
                );
            }
        );
    }
);